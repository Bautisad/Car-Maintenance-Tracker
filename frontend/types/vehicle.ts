export type Vehicle = {
  id: number;
  year: number;
  make: string;
  model: string;
  currentMileage: number;
};

export type VehicleInput = {
  year: number;
  make: string;
  model: string;
  currentMileage: number;
};